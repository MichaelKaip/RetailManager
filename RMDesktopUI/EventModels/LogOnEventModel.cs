

namespace RMDesktopUI.EventModels
{
    /*
     * Class is empty, because it's only needed to pass an instance of into
     * _event.PublishOnUIThread(new LogOnEventModel()).
     *
     * So, therefore it can be differentiated from other calls very easily.
     */
    public class LogOnEventModel
    {
    }
}